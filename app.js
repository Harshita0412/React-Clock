import React from 'react';
import Clock from './Clock'; // Ensure correct path if the file is in a different folder

const App = () => {
  return (
    <div>
      <Clock />
    </div>
  );
};

export default App;
